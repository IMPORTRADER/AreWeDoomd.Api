# M7: Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** Close phase 1: decision → full prompt/response drill-down, plus the accumulated small follow-ups. Spec §11 M7 + ledger-carried minors.

## Global Constraints
- Backend: AreWeDoomd.Api, branch feature/m3-admin-read. **appsettings.json = operator's uncommitted edit — never touch/stage; explicit-path staging only.** Frontend: AreWeDoomd.UI/arewedoomd-admin, branch doga/ai-dashboard. Commits plain, no trailer; hook-blocked bash commit → PowerShell tool.
- Session-log security: sessionLogRef is `{yyyy-MM-dd}/{fileName}.txt` relative to `{DecisionLog RootPath}/../ai-sessions`... ACTUALLY: sessions live at the sibling `ai-sessions` dir under the shared volume root (AgentService writes `logs/ai-sessions/...`; DecisionLog:RootPath is `.../agent-decisions`). Reader resolves: `Path.Combine(Path.GetDirectoryName(rootPath), "ai-sessions", ref)`. PATH TRAVERSAL MUST BE IMPOSSIBLE: validate ref against strict regex `^\d{4}-\d{2}-\d{2}/[A-Za-z0-9_\-]+\.txt$` AND verify the resolved full path stays under the ai-sessions root (Path.GetFullPath prefix check). Missing file/dir → NotFound, never 500. Read with FileShare.ReadWrite. Content returned as plain text (capped at 256 KB defensively).
- Verify per task: backend build + unit suite green (327 at start); integration 15/1-known. UI lint 0 + tests green (88 at start).

### Task 1 (API): session-log read endpoint
Files: Application `Common/Interfaces/ISessionLogReader.cs` (`Task<string?> ReadAsync(string sessionRef, CancellationToken ct)` — null = not found/invalid) + Infrastructure `FileSessionLogReader` (options reuse DecisionLogOptions.RootPath sibling resolution; traversal-proof per Global Constraints; TDD: valid read, traversal attempts `../`, absolute path, bad chars → null, missing file → null, oversized file truncated) + Application query slice `Features/AiManagement/Queries/GetSessionLog/` (`GetSessionLogQuery(string SessionRef)` → Result<SessionLogResult(string Content)>; invalid/missing → NotFound "session_log.not_found") + controller GET `api/admin/session-logs?ref=...` (Admin policy class) + Contracts DTO + postman. Commit: `feat: add session log drill-down endpoint`

### Task 2 (UI): drill-down in DecisionRow + LoginPage detail-first
- Service `getSessionLog(ref)`; hook `useSessionLog` (lazy fetch on expand-click); DecisionRow expanded view: when sessionLogRef present, "Prompt/yanıtı gör" toggle → fetches + renders content in a scrollable `<pre>` (max-h, token colors, monospace); loading/error/notfound states.
- LoginPage extractApiError → detail-first (ledger minor).
- Tests: hook + row-expand rendering. Commit: `feat: add prompt drill-down to the decision feed`

### Task 3 (API+UI micro-fixes + docs + sweep)
- Rebuild snapshot: `Requested` from records count is fine but flag stays — instead set `Status="completed"`, keep `Requested=created count`, ADD `rebuilt:true` already present; leave as-is, just document in code comment (no change beyond comment).
- Poll-gap double-submit: useBulkCreate.start seeds optimistic `{ jobId, status:'queued', requested:count, generated:0, created:0, failed:[], createdUsers:[] }` immediately on 202 (modal leaves phase 1 instantly). Test updated.
- Root `AreWeDoomd/CLAUDE.md` (OUTER repo — not a git repo; just edit the file): correct "Gemini or Anthropic" → "Gemini or OpenRouter".
- Sweeps: backend build+both test suites; UI lint/test/build; greps (traversal regex present; no stray secrets). Commits: `fix: seed optimistic bulk job state on start` (UI) + `chore: finish M7 polish` (API, only if changes).

## Self-Review Notes
- Trait-filter chips already shipped in M4 (clickable chips set the filter) — no work.
- Retention-config surfacing deferred permanently (YAGNI at current scale) — noted.
- CountAsync single-query optimization skipped (fleet tiny; ledger-noted).
